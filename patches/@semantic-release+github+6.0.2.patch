diff --git a/node_modules/@semantic-release/github/index.js b/node_modules/@semantic-release/github/index.js
index 5ce76be..3ce02f1 100644
--- a/node_modules/@semantic-release/github/index.js
+++ b/node_modules/@semantic-release/github/index.js
@@ -12,6 +12,7 @@ let verified;
 async function verifyConditions(pluginConfig, context) {
   const {options} = context;
   // If the GitHub publish plugin is used and has `assets`, `successComment`, `failComment`, `failTitle`, `labels` or `assignees` configured, validate it now in order to prevent any release if the configuration is wrong
+  console.log('github plugin: has options', options.publish);
   if (options.publish) {
     const publishPlugin =
       castArray(options.publish).find(config => config.path && config.path === '@semantic-release/github') || {};
@@ -34,6 +35,7 @@ async function publish(pluginConfig, context) {
     verified = true;
   }
 
+  console.log('github plugin >>> publish!')
   return publishGitHub(pluginConfig, context);
 }
 
diff --git a/node_modules/@semantic-release/github/lib/publish.js b/node_modules/@semantic-release/github/lib/publish.js
index 32b66fa..da26198 100644
--- a/node_modules/@semantic-release/github/lib/publish.js
+++ b/node_modules/@semantic-release/github/lib/publish.js
@@ -23,6 +23,7 @@ module.exports = async (pluginConfig, context) => {
   const github = getClient({githubToken, githubUrl, githubApiPathPrefix, proxy});
   const release = {owner, repo, tag_name: gitTag, name, body: notes, prerelease: isPrerelease(branch)};
 
+  console.log('github plugin, release', release)
   debug('release object: %O', release);
 
   // When there are no assets, we publish a release directly
